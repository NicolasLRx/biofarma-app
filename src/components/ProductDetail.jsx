import React from 'react';

function ProductDetail({ match }) {
 
/*   const productId = match.params.id; */

  return <div>Product Detail Component for ID: {productId}</div>;
}

export default ProductDetail;